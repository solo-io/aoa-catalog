apiVersion: gateway.solo.io/v1
kind: RouteOption
metadata:
  name: petstore-route-policies
  namespace: gloo-system
spec:
  options:
    jwt:
      disable: true
                
                
    