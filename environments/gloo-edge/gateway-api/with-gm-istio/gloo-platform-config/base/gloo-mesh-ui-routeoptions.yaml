apiVersion: gateway.solo.io/v1
kind: RouteOption
metadata:
  name: gmui-route-policies
  namespace: gloo-mesh
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: gloo-mesh-ui-https
  options:
    jwt:
      disable: true

