apiVersion: gateway.solo.io/v1
kind: RouteOption
metadata:
  name: general-chatbot-route-policies
  namespace: ai-chatbot
spec:
  options:
    extauth:
      configRef:
        name: general-chatbot-okta
        namespace: gloo-system