apiVersion: gateway.solo.io/v1
kind: RouteOption
metadata:
  name: portal-frontend-route-policies
  namespace: gloo-system
spec:
  options:
    jwt:
      disable: true
                
                
    