apiVersion: gateway.solo.io/v1
kind: RouteOption
metadata:
  name: portal-frontend-auth-policy
  namespace: gloo-system
spec:
  options:
    extauth:
      configRef:
        name: portal-frontend-authconfig
        namespace: gloo-system
                
                
    