apiVersion: gateway.solo.io/v1
kind: RouteOption
metadata:
  name: portal-backend-route-policies
  namespace: gloo-system
spec:
  options:
    jwt:
      disable: true
                
                
    