apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gloo-gateway-ui-helm
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io/solo-io
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: gloo-system
  project: default
  source:
    chart: gloo-platform
    helm:
      skipCrds: true
      values: |
        featureGates:
            insightsConfiguration: true
        common:
            adminNamespace: "gloo-system"
            cluster: mgmt
        glooInsightsEngine:
            enabled: true
            runAsSidecar: false
        glooAnalyzer:
            enabled: true
            runAsSidecar: false
        glooUi:
            enabled: true
            serviceType: LoadBalancer
            prometheusUrl: http://prometheus-server.gloo-mesh.svc.cluster.local
        licensing:
            licenseSecretName: "gloo-license"
        # This disables discovery since it's fighting with the one from the gloo-mesh namespace
        glooAgent:
            enabled: false
            deploymentOverrides:
                spec:
                    template:
                        replicas: 0
        prometheus:
            enabled: false
            skipAutoMigration: true
        redis:
            deployment:
                enabled: false
        telemetryCollector:
            enabled: true
            mode: deployment
            replicaCount: 1
        telemetryCollectorCustomization:
            pipelines:
              logs/analyzer:
                enabled: false
              logs/ui:
                enabled: false
              metrics/ui:
                enabled: true
                pipeline:
                  exporters:
                  - prometheus

    repoURL: https://storage.googleapis.com/gloo-platform-dev/platform-charts/helm-charts
    targetRevision: 2.6.8-2025-01-30-v2.6.x-7283cf42d2
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
  # ignore the self-signed certs that are being generated automatically    
  ignoreDifferences:
  - group: v1
    kind: Secret
  - kind: ClusterRole
  - kind: ClusterRoleBinding