apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: httpbin-fault-injection-delay-delegate-route
  namespace: httpbin
spec:
  rules:
    - matches:
      - path:
          type: Exact
          value: /anything/delay
      backendRefs:
        - name: httpbin
          port: 8000
          weight: 100
      filters:
        - type: ExtensionRef
          extensionRef:
            group: gateway.solo.io
            kind: RouteOption
            name: httpbin-fault-injection-delay-route-policies