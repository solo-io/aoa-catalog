apiVersion: gateway.solo.io/v1
kind: RouteOption
metadata:
  name: kagent-ui-route-policies
  namespace: kagent
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: kagent-ui-route
  options:
    jwt:
      disable: true
                
    