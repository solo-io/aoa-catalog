apiVersion: gateway.solo.io/v1
kind: RouteOption
metadata:
  name: kagent-ui-ext-auth
  namespace: kagent
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: kagent-ui-route
  options:
    extauth:
      configRef:
        name: kagent-ui-gcp
        namespace: gloo-system
