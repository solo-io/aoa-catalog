apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gloo-platform-portal
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: gloo-mesh
  project: default
  source:
    chart: gloo-platform
    helm:
      skipCrds: true
      values: |
        common:
            addonNamespace: "gloo-mesh-addons"
            adminNamespace: "gloo-mesh"
            cluster: mgmt
        glooPortalServer:
            enabled: true
            apiKeyStorage:
                secretKey: xyz987
                type: redis
                redis:
                    address: redis.gloo-mesh-addons:6379
                    enabled: true
        redis:
            deployment:
                enabled: true
        clickhouse:
            enabled: false
            persistence:
                enabled: false
    repoURL: https://storage.googleapis.com/gloo-platform-dev/platform-charts/helm-charts
    targetRevision: 2.6.0-beta3-2024-05-30-main-1d26e8b265
  syncPolicy:
    automated:
      prune: true
      selfHeal: true