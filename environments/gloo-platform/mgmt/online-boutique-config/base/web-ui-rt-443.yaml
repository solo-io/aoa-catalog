apiVersion: networking.gloo.solo.io/v2
kind: RouteTable
metadata:
  name: frontend-routetable
  namespace: web-team
spec:
  hosts:
    - 'shop.east.canary.glooplatform.com'
    - 'shop.central.canary.glooplatform.com'
  virtualGateways:
    - name: north-south-gw
      namespace: istio-gateways
      cluster: mgmt
  workloadSelectors: []
  http:
    - name: frontend
      labels:
        virtual-destination: frontend
        route_name: frontend
      matchers:
      - uri:
          prefix: /
      forwardTo:
        destinations:
          - ref:
              name: frontend
              namespace: web-team
            kind: VIRTUAL_DESTINATION
            port:
              number: 80
#---
#apiVersion: networking.gloo.solo.io/v2
#kind: RouteTable
#metadata:
#  name: frontend-central
#  namespace: web-team
#spec:
#  hosts:
#    - 'shop.central.canary.glooplatform.com'
#  virtualGateways:
#    - name: north-south-gw
#      namespace: istio-gateways
#      cluster: mgmt
#  workloadSelectors: []
#  http:
#    - name: frontend
#      labels:
#        virtual-destination: frontend
#        route_name: frontend
#      matchers:
#      - uri:
#          prefix: /
#      forwardTo:
#        destinations:
#          - ref:
#              name: frontend
#              namespace: web-team
#            kind: VIRTUAL_DESTINATION
#            port:
#              number: 80

    