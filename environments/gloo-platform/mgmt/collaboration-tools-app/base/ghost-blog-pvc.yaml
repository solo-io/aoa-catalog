apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ghost-content
  namespace: collaboration-tools
  labels:
    app: blog
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  #storageClassName: civo-volume