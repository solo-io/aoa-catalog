apiVersion: admin.gloo.solo.io/v2
kind: Workspace
metadata:
  name: code-server
  namespace: gloo-mesh
spec:
  workloadClusters:
  - name: 'mgmt'
    namespaces:
    - name: code-server-team-config
  - name: '*'
    namespaces:
    - name: code-server