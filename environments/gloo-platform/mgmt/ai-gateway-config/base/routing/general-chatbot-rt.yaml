apiVersion: networking.gloo.solo.io/v2
kind: RouteTable
metadata:
  name: general-chatbot-routetable
  namespace: ai-gateway-ws-config
spec:
  hosts:
    - 'general-chatbot.central.glooplatform.com'
  virtualGateways:
    - name: north-south-gw
      namespace: istio-gateways
      cluster: mgmt
  workloadSelectors: []
  http:
    - name: chatbot-client
      labels:
        oauth: general-chatbot.central.glooplatform.com
      matchers:
      - uri:
          prefix: /callback
      - uri:
          prefix: /
      forwardTo:
        destinations:
        - ref:
            name: general-chat
            namespace: ai-chatbot
            cluster: glooplatform-central
          port:
            number: 3000