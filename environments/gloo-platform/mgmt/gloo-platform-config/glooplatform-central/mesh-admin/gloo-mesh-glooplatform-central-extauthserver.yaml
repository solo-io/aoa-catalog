apiVersion: admin.gloo.solo.io/v2
kind: ExtAuthServer
metadata:
  name: glooplatform-central-ext-auth-server
  namespace: gloo-mesh
spec:
  destinationServer:
    ref:
      cluster: glooplatform-central
      name: ext-auth-service
      namespace: gloo-mesh-addons
    port:
      name: grpc
  #destinationServer:
  #  kind: VIRTUAL_DESTINATION
  #  ref:
  #    cluster: mgmt
  #    name: ext-auth-service-central-central
  #    namespace: gloo-mesh-addons