apiVersion: trafficcontrol.policy.gloo.solo.io/v2
kind: TransformationPolicy
metadata:
  name: modify-foo-header
  namespace: httpbin
spec:
  applyToRoutes:
  - route:
      labels:
        transform-header: foo
  config:
    phase:
      preAuthz:
        priority: 1
    request:
      injaTemplate:
        extractors:
          foo:
            header: 'foo'
        headers:
          bar:
            text: "{{ foo }}"