apiVersion: trafficcontrol.policy.gloo.solo.io/v2
kind: TransformationPolicy
metadata:
  name: modify-header
  namespace: httpbin
spec:
  applyToRoutes:
  - route:
      labels:
        transform-header: foo
  config:
    request:
      injaTemplate:
        extractors:
          foo:
            header: 'foo'
            regex: '*'
        headers:
          bar:
            text: "{{ foo }}"