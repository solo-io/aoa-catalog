apiVersion: networking.gloo.solo.io/v2
kind: VirtualDestination
metadata:
  name: httpbin-green-vd
  namespace: httpbin-team-config
  labels:
    failover: "true"
spec:
  hosts:
  - httpbin-green.randomvalue
  services:
  - labels:
      app: httpbin-green
  ports:
    - number: 8000
      protocol: HTTP