apiVersion: admin.gloo.solo.io/v2
kind: Workspace
metadata:
  name: httpbin
  namespace: gloo-mesh
spec:
  workloadClusters:
  - name: 'mgmt'
    namespaces:
    - name: httpbin-team-config
  - name: '*'
    namespaces:
    - name: httpbin-blue
    - name: httpbin-green