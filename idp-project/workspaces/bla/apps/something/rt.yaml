apiVersion: networking.gloo.solo.io/v2
kind: RouteTable
metadata:
  name: something
  namespace: bla-config
spec:
  hosts:
    - 'something.bla.idp.glooplatform.com'
  virtualGateways:
    - name: north-south-gw
      namespace: istio-gateways
      cluster: mgmt
  http:
  - name: something
    matchers:
    - uri:
        prefix: /
    forwardTo:
      pathRewrite: /
      destinations:
      - ref:
          name: something
          namespace: bla-config
        kind: VIRTUAL_DESTINATION
        port:
          number: 8080
---
