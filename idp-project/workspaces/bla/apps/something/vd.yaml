apiVersion: networking.gloo.solo.io/v2
kind: VirtualDestination
metadata:
  name: something
  namespace: bla-config
spec:
  hosts:
  - something.bla.solo-io.mesh
  services:
  - labels:
      app: something
    namespace: bla
  ports:
  - number: 8080
    protocol: HTTP
    targetPort:
      name: http