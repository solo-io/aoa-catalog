apiVersion: admin.gloo.solo.io/v2
kind: Workspace
metadata:
  name: idp-project
  namespace: gloo-mesh
spec:
  workloadClusters:
  - name: '*'
    namespaces:
    - name: idp-project
    - name: idp-project-config