apiVersion: networking.gloo.solo.io/v2
kind: VirtualDestination
metadata:
  name: testing9
  namespace: team1-config
spec:
  hosts:
  - testing9.team1.solo-io.mesh
  services:
  - labels:
      app: testing9
    namespace: team1
  ports:
  - number: 8080
    protocol: HTTP
    targetPort:
      name: http