apiVersion: networking.gloo.solo.io/v2
kind: RouteTable
metadata:
  name: testing9
  namespace: team1-config
spec:
  hosts:
    - 'testing9.team1.idp.glooplatform.com'
  virtualGateways:
    - name: north-south-gw
      namespace: istio-gateways
      cluster: mgmt
  http:
  - name: testing9
    matchers:
    - uri:
        prefix: /
    forwardTo:
      pathRewrite: /
      destinations:
      - ref:
          name: testing9
          namespace: team1-config
        kind: VIRTUAL_DESTINATION
        port:
          number: 8080
---
