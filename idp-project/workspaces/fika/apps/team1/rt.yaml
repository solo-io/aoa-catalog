apiVersion: networking.gloo.solo.io/v2
kind: RouteTable
metadata:
  name: team1
  namespace: fika-config
spec:
  hosts:
    - 'team1.fika.idp.glooplatform.com'
  virtualGateways:
    - name: north-south-gw
      namespace: istio-gateways
      cluster: mgmt
  http:
  - name: team1
    matchers:
    - uri:
        prefix: /
    forwardTo:
      pathRewrite: /
      destinations:
      - ref:
          name: team1
          namespace: fika-config
        kind: VIRTUAL_DESTINATION
        port:
          number: 8080
---
