apiVersion: networking.gloo.solo.io/v2
kind: VirtualDestination
metadata:
  name: test1
  namespace: fika-config
spec:
  hosts:
  - test1.fika.solo-io.mesh
  services:
  - labels:
      app: test1
    namespace: fika
  ports:
  - number: 8080
    protocol: HTTP
    targetPort:
      name: http