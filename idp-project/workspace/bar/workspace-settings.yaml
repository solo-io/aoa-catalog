apiVersion: admin.gloo.solo.io/v2
kind: WorkspaceSettings
metadata:
  name: "bar"
  namespace: bar-config
spec:
  exportTo:
  - workspaces:
    - name: ops-team
  importFrom:
  - workspaces:
    - name: ops-team
  options:
    serviceIsolation: 
      enabled: false
      trimProxyConfig: false
